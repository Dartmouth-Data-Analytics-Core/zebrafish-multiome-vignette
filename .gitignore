.Rproj.user
.Rhistory
.RData
.Ruserdata

/.quarto/
**/*.quarto_ipynb

# Quarto
/_site/
# NOTE: _freeze/ is intentionally NOT ignored - it must be committed
# so CI can rebuild the book without re-running long chapters.
